// Service removed as API key/Env vars are not required for this version.
// This prevents "process is not defined" errors in the browser build.

export const analyzeImage = async () => {
  console.warn("AI analysis is currently disabled.");
  return "";
};